{/* DO NOT EDIT! */}
{/* Automatically generated from docusaurus-template-liquid/templates/docusaurus. */}

import CodeBlock from '@theme/CodeBlock';

{/* ------------------------------------------------------------------------ */}

### Check npm package tags

Execute the following command in a terminal:

```sh
npm dist-tag ls @micro-os-plus/diag-trace
```

### Tag the npm package as `latest`

When the release is considered stable, promote it as `latest`:

<CodeBlock language="sh"> {
`npm dist-tag add @micro-os-plus/diag-trace@4.2.3 latest
`} </CodeBlock>

Check the result:

<CodeBlock language="sh"> {
`npm dist-tag ls @micro-os-plus/diag-trace
`} </CodeBlock>

Should the release prove problematic and require unpublishing:

<CodeBlock language="sh"> {
`npm unpublish @micro-os-plus/diag-trace@4.2.3
`} </CodeBlock>
